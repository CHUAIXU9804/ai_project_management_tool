psycopg[binary]>=3.2,<4
python-dotenv>=1.0,<2

# Stage 0 - Connect & Authorize (Google OAuth for Gmail + Google Calendar)
Flask>=3.0,<4
google-auth>=2.30,<3
google-auth-oauthlib>=1.2,<2
requests>=2.31,<3
cryptography>=42,<44

# Stage 4 - Embed & detect relationships (local embeddings; pgvector for storage)
sentence-transformers>=3.0,<6
